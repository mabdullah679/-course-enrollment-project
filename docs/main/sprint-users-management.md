Deep instructions for the Users grid (Admin/Staff):

Combined server filtering semantics (see instruction.md).

Client-side CSV export fallback: include columns name, email, username, role, approved, active, createdAt. Respect current filters and search.

Modal behaviors: role/status patch; optimistic UI update; reconcile on re-fetch.

Search: update instantly in UI, submit triggers API with q.